// Without this file Gradle derives the root project name (and therefore the published
// artifactId) from the checkout directory name, which makes the Maven coordinate depend
// on where the repository happens to be cloned. Pin it explicitly.
//
// Published coordinate:  io.github.jaffe2718:whisper-jni-custom:<version from gradle.properties>
rootProject.name = 'whisper-jni-custom'
