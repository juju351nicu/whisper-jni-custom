# Library version.
# Convention: <whisper.cpp version>-<build number of this wrapper>
# NOTE: intentionally different from the previous 1.0.1 (whisper.cpp 1.8.3) so that both
#       can coexist in ~/.m2 and downstream projects switch over explicitly.
version=1.9.3-1

# whisper.cpp tag the submodule (src/main/native/whisper) is pinned to.
# Informational only - the actual pin lives in git.
whisperCppVersion=1.9.3

# Test dependency versions
junitVersion=5.14.4
