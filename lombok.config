# このプロジェクトの Lombok 設定。親ディレクトリの lombok.config は探索しない
config.stopBubbling = true

# 生成コードに @lombok.Generated を付ける（JaCoCo などのカバレッジ計測から除外できる）
lombok.addLombokGeneratedAnnotation = true

# Java 9+ では java.beans.ConstructorProperties を付けない
lombok.anyConstructor.addConstructorProperties = false
