name: Build and publish
on:
    workflow_dispatch:
jobs:
    download-models:
        runs-on: ubuntu-latest
        steps:
            -   uses: actions/checkout@v4
            -   name: Download tiny ggml model
                run: |
                    git submodule update --init
                    ./scripts/download-test-model.sh
                    ./scripts/download-vad-model.sh
                    # Move models together for simpler download
                    mkdir models
                    mv ggml-tiny.bin models/
                    mv ggml-silero-v6.2.0.bin models/
            -   uses: actions/upload-artifact@v4
                with:
                    name: test-models
                    path: models/*.bin
                    retention-days: 2

    build-linux:
        needs: [ download-models ]
        strategy:
            matrix:
                # arch is used the artifact name
                include:
                    -   platform: ubuntu-22.04-arm
                        arch: arm64
                        vulkan: ON
                    -   platform: ubuntu-22.04-arm
                        arch: arm64
                        vulkan: OFF
                    -   platform: ubuntu-22.04
                        arch: x64
                        vulkan: ON
                    -   platform: ubuntu-22.04
                        arch: x64
                        vulkan: OFF
        runs-on: ${{ matrix.platform }}
        steps:
            # Checkout HAS to come before downloading artifacts, learned that the hard way
            -   uses: actions/checkout@v4
            -   uses: actions/download-artifact@v4
                with:
                    name: test-models
            -   uses: actions/setup-java@v4
                with:
                    java-version: 25
                    distribution: 'temurin'
            -   name: Install musl libc toolchain (x64/ARM64)
                if: matrix.vulkan == 'OFF'
                run: |
                    # 1. Clean up existing musl directory and create fresh installation directory
                    sudo rm -rf /opt/musl && sudo mkdir -p /opt/musl
                    
                    # 2. Determine target architecture (map matrix arch to musl arch naming)
                    if [ "${{ matrix.arch }}" = "arm64" ]; then
                      MUSL_ARCH="aarch64"
                    else
                      MUSL_ARCH="x86_64"
                    fi
                    
                    # 3. Install musl cross compiler
                    WGET_URL="https://github.com/musl-cc/musl.cc/releases/download/v0.0.1/${MUSL_ARCH}-linux-musl-native.tgz"
                    wget $WGET_URL -O /tmp/musl-native.tgz
                    sudo tar -zxf /tmp/musl-native.tgz -C /opt/musl --strip-components=1
                    
                    # 4. Clean up temporary downloaded archive file
                    rm -f /tmp/musl-native.tgz
                    
                    # 5. Configure environment variables for musl cross compiler
                    echo "MUSL_ROOT=/opt/musl"                           >> $GITHUB_ENV
                    echo "CC=/opt/musl/bin/${MUSL_ARCH}-linux-musl-gcc"  >> $GITHUB_ENV
                    echo "CXX=/opt/musl/bin/${MUSL_ARCH}-linux-musl-g++" >> $GITHUB_ENV
                    echo "PATH=/opt/musl/bin:$PATH"                      >> $GITHUB_ENV
                    echo "MUSL_ARCH=${MUSL_ARCH}"                        >> $GITHUB_ENV
                    
                    # 6. Verify musl cross compiler installation
                    /opt/musl/bin/${MUSL_ARCH}-linux-musl-gcc --version
                    /opt/musl/bin/${MUSL_ARCH}-linux-musl-g++ --version
                    ls -l /opt/musl/lib/libc.so

            -   name: Install patchelf
                if: matrix.vulkan == 'OFF'
                run: |
                    sudo apt update -y && sudo apt install -y patchelf
                    patchelf --version

            -   name: Install Vulkan Tools
                # Install Glslang because Vulkan doesn't bundle it for Ubuntu
                if: matrix.vulkan == 'ON'
                run: |
                    sudo apt update
                    sudo apt install -y vulkan-tools glslang-tools
                    sudo apt-get install -y libvulkan1 mesa-vulkan-drivers
                    sudo vulkaninfo

            -   name: Build shaderc
                if: matrix.vulkan == 'ON'
                run: |
                    # Set up shaderc source directory and build directory
                    SOURCE_DIR=$PWD/shaderc
                    BUILD_DIR=$PWD/shaderc_build
                    
                    # Clone shaderc repository
                    git clone https://github.com/google/shaderc $SOURCE_DIR
                    cd $SOURCE_DIR
                    ./utils/git-sync-deps  # This fetches the dependencies
                    
                    # Create a separate build directory
                    mkdir $BUILD_DIR
                    cd $BUILD_DIR
                    
                    # Build shaderc using CMake and Ninja
                    cmake -GNinja -DCMAKE_BUILD_TYPE=Release $SOURCE_DIR
                    ninja
                    
                    # Optionally run tests
                    # ctest
                    
                    # Move glslc to a directory in PATH
                    sudo mv glslc/glslc /usr/local/bin/

            -   name: Prepare Vulkan SDK
                uses: humbletim/setup-vulkan-sdk@v1.2.1
                if: matrix.vulkan == 'ON'
                with:
                    vulkan-query-version: 1.4.304.1
                    vulkan-components: Vulkan-Headers, Vulkan-Loader
                    vulkan-use-cache: true

            -   name: Setup project dir
                run: |
                    git submodule update --init
                    mv ./ggml-silero-v6.2.0.bin ./src/main/resources
            -   name: Build
                env:
                    VULKAN: ${{ matrix.vulkan }}
                run: ./scripts/build-linux.sh

            -   name: Test
                run: ./gradlew test
            -   name: Publish test report
                uses: mikepenz/action-junit-report@v5
                if: success() || failure()
                with:
                    report_paths: '**/test-results/**/*.xml'

            -   name: Upload natives
                uses: actions/upload-artifact@v4
                with:
                    name: linux-${{ matrix.arch }}-${{ matrix.vulkan }}-natives
                    path: whisperjni-build/*

    build-windows:
        needs: [ download-models ]
        runs-on: windows-latest
        strategy:
            matrix:
                vulkan: [ OFF, ON ]
        steps:
            -   uses: actions/checkout@v4
            -   uses: actions/download-artifact@v4
                with:
                    name: test-models
            -   uses: actions/setup-java@v4
                with:
                    java-version: 25
                    distribution: 'temurin'
            # Also install Vulkan if we're building for it
            -   name: Install Vulkan SDK
                uses: jakoch/install-vulkan-sdk-action@v1
                if: matrix.vulkan == 'ON'
                with:
                    vulkan_version: 1.4.304.1
                    install_runtime: true
                    # Does using cache fuck up installing the runtime??
                    # ^ yes, it seems so
                    # cache: true
            -   name: Setup project dir
                run: |
                    git submodule update --init
                    mv ./ggml-silero-v6.2.0.bin ./src/main/resources
            -   name: Build
                shell: powershell
                env:
                    VULKAN: ${{ matrix.vulkan }}
                run: .\scripts\build-windows.ps1
            -   name: Test
                run: ./gradlew test
            -   name: Publish test report
                uses: mikepenz/action-junit-report@v5
                if: success() || failure()
                with:
                    report_paths: '**/build/test-results/**/*.xml'
            -   name: Upload natives
                uses: actions/upload-artifact@v4
                with:
                    name: windows-x64-${{ matrix.vulkan }}-natives
                    path: whisperjni-build/*.dll

    build-mac:
        needs: [ download-models ]
        strategy:
            matrix:
                include:
                    -   arch: x64
                        version: macos-15-intel
                        vulkan: ON
                    -   arch: x64
                        version: macos-15-intel
                        vulkan: OFF
                    -   arch: arm64
                        version: macos-14
                        vulkan: ON
                    -   arch: arm64
                        version: macos-14
                        vulkan: OFF
        # Can't we just build on macos-14 for both?? Need to test
        runs-on: ${{ matrix.version }}
        steps:
            -   uses: actions/checkout@v4
            -   uses: actions/download-artifact@v4
                with:
                    name: test-models
            -   uses: actions/setup-java@v4
                with:
                    java-version: 25
                    distribution: 'temurin'
            -   name: Install Vulkan SDK
                uses: humbletim/setup-vulkan-sdk@v1.2.1
                if: matrix.vulkan == 'ON'
                with:
                    vulkan-query-version: latest
                    vulkan-components: Vulkan-Headers, Vulkan-Loader
                    vulkan-use-cache: true
            -   name: Install shaderc
                if: matrix.vulkan == 'ON'
                # This envelops glslc
                run: brew install shaderc
            -   name: Setup project dir
                run: |
                    git submodule update --init
                    mv ./ggml-silero-v6.2.0.bin ./src/main/resources
            -   name: Build
                env:
                    VULKAN: ${{ matrix.vulkan }}
                run: |
                    export DYLD_LIBRARY_PATH=/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/lib/
                    # Just in case
                    chmod +x ./scripts/build-mac.sh
                    ./scripts/build-mac.sh ${{ matrix.arch }}
                # Apparently gradlew isn't executable by default
            -   name: Test
                run: |
                    chmod +x ./gradlew
                    ./gradlew test
            -   name: Publish test report
                uses: mikepenz/action-junit-report@v5
                if: success() || failure()
                with:
                    report_paths: '**/build/test-results/**/*.xml'
            -   name: Artifact macos x64
                uses: actions/upload-artifact@v4
                with:
                    name: mac-${{ matrix.arch }}-${{ matrix.vulkan }}-natives
                    path: whisperjni-build/*.dylib

    win-cublas-blas:
        needs: [build-windows]
        runs-on: windows-latest
        steps:
            -   uses: actions/checkout@v4
            -   uses: actions/download-artifact@v4
                with:
                    name: windows-x64-OFF-natives
                    path: windows-x64-base
            -   name: download whisper.cpp libs
                shell: powershell
                run: |
                    Invoke-WebRequest -Uri "https://github.com/ggml-org/whisper.cpp/releases/download/v1.9.2/whisper-cublas-12.4.0-bin-x64.zip" -OutFile "whisper-cublas-12.4.0-bin-x64.zip"
                    Expand-Archive -Path "whisper-cublas-12.4.0-bin-x64.zip" -DestinationPath "./whisper-cublas-12.4.0-bin-x64"
                    Invoke-WebRequest -Uri "https://github.com/ggml-org/whisper.cpp/releases/download/v1.9.2/whisper-blas-bin-x64.zip" -OutFile "whisper-blas-bin-x64.zip"
                    Expand-Archive -Path "whisper-blas-bin-x64.zip" -DestinationPath "./whisper-blas-bin-x64"
            -   name: copy win cublas
                shell: powershell
                run: |
                    New-Item -ItemType Directory -Path "windows-x64-cublas-12.4.0-natives"
                    Copy-Item -Path "windows-x64-base\whisper-jni.dll" -Destination "windows-x64-cublas-12.4.0-natives\whisper-jni.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\whisper.dll" -Destination "windows-x64-cublas-12.4.0-natives\whisper.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\ggml.dll" -Destination "windows-x64-cublas-12.4.0-natives\ggml.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\ggml-base.dll" -Destination "windows-x64-cublas-12.4.0-natives\ggml-base.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\ggml-cpu.dll" -Destination "windows-x64-cublas-12.4.0-natives\ggml-cpu.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\ggml-cuda.dll" -Destination "windows-x64-cublas-12.4.0-natives\ggml-cuda.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\cudart64_12.dll" -Destination "windows-x64-cublas-12.4.0-natives\cudart64_12.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\cublas64_12.dll" -Destination "windows-x64-cublas-12.4.0-natives\cublas64_12.dll"
                    Copy-Item -Path "whisper-cublas-12.4.0-bin-x64\Release\cublasLt64_12.dll" -Destination "windows-x64-cublas-12.4.0-natives\cublasLt64_12.dll"
            -   name: copy win blas
                shell: powershell
                run: |
                    New-Item -ItemType Directory -Path "windows-x64-blas-natives"
                    Copy-Item -Path "windows-x64-base\whisper-jni.dll" -Destination "windows-x64-blas-natives\whisper-jni.dll"
                    Copy-Item -Path "whisper-blas-bin-x64\Release\whisper.dll" -Destination "windows-x64-blas-natives\whisper.dll"
                    Copy-Item -Path "whisper-blas-bin-x64\Release\ggml.dll" -Destination "windows-x64-blas-natives\ggml.dll"
                    Copy-Item -Path "whisper-blas-bin-x64\Release\ggml-base.dll" -Destination "windows-x64-blas-natives\ggml-base.dll"
                    Copy-Item -Path "whisper-blas-bin-x64\Release\ggml-cpu.dll" -Destination "windows-x64-blas-natives\ggml-cpu.dll"
                    Copy-Item -Path "whisper-blas-bin-x64\Release\ggml-blas.dll" -Destination "windows-x64-blas-natives\ggml-blas.dll"
                    Copy-Item -Path "whisper-blas-bin-x64\Release\libopenblas.dll" -Destination "windows-x64-blas-natives\libopenblas.dll"
            -   name: Upload win cublas
                uses: actions/upload-artifact@v4
                with:
                    name: windows-x64-cublas-12.4.0-natives
                    path: windows-x64-cublas-12.4.0-natives/*
            -   name: Upload win blas
                uses: actions/upload-artifact@v4
                with:
                    name: windows-x64-blas-natives
                    path: windows-x64-blas-natives/*

    linux-cuda:
        runs-on: ubuntu-22.04
        steps:
            -   uses: actions/checkout@v4
            -   uses: jlumbroso/free-disk-space@main
                with:
                    tool-cache: false
                    android: true
                    dotnet: true
                    haskell: true
                    docker-images: true

            -   uses: actions/setup-java@v4
                with:
                    java-version: 25
                    distribution: 'temurin'

            -   name: Setup project dir
                run: |
                    git submodule update --init
                    curl -o ./ggml-silero-v6.2.0.bin https://huggingface.co/ggml-org/whisper-vad/resolve/main/ggml-silero-v6.2.0.bin
                    mv ./ggml-silero-v6.2.0.bin ./src/main/resources
                    curl -o ./ggml-tiny.bin https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin

            -   name: Install CUDA
                run: |
                    wget https://developer.download.nvidia.com/compute/cuda/12.4.0/local_installers/cuda_12.4.0_550.54.14_linux.run
                    sudo sh cuda_12.4.0_550.54.14_linux.run --silent --toolkit --override
                    echo "CUDA_PATH=/usr/local/cuda-12.4" >> $GITHUB_ENV
                    echo "LD_LIBRARY_PATH=/usr/local/cuda-12.4/lib64" >> $GITHUB_ENV
                    echo "PATH=$PATH:/usr/local/cuda-12.4/bin" >> $GITHUB_ENV
                    echo "CUDACXX=/usr/local/cuda-12.4/bin/nvcc" >> $GITHUB_ENV

            -   name: Build Linux CUDA
                shell: bash
                run: |
                    chmod +x ./scripts/build-cuda.sh
                    ./scripts/build-cuda.sh

            -   name: Upload native library
                uses: actions/upload-artifact@v4
                with:
                    name: linux-x64-cuda-natives
                    path: whisperjni-build/*
            # cannot test because there is no CUDA device on GitHub Actions

    publish:
        runs-on: ubuntu-latest
        needs: [ download-models, build-linux, build-windows, build-mac, linux-cuda, win-cublas-blas ]
        steps:
            -   uses: actions/checkout@v4
            -   uses: actions/download-artifact@v4
                with:
                    # Put everything into /natives
                    path: natives
                    pattern: "*-natives"
            # We still need the silero model
            -   uses: actions/download-artifact@v4
                with:
                    name: test-models
            -   uses: actions/setup-java@v4
                with:
                    java-version: 25
                    distribution: 'temurin'
            -   name: Check tree
                run: ls -R
            -   name: Prepare artifacts
                run: |
                    echo "Moving VAD model"
                    mv ./ggml-silero-v6.2.0.bin ./src/main/resources
                    # Now prepare the natives
                    echo "Renaming native folders to prod names"
                    for native_folder in natives/*/; do
                        # Get the base name of the native folder
                        original_name=$(basename "$native_folder")
                        native_folder_name="$original_name"
                    
                        # Rename the native folder based on the suffix
                        if [[ "$native_folder_name" == *"-ON-natives" ]]; then
                            native_folder_name="${native_folder_name/-ON-natives/-vulkan-natives}"
                        elif [[ "$native_folder_name" == *"-OFF-natives" ]]; then
                            native_folder_name="${native_folder_name/-OFF-natives/-natives}"
                        else
                            echo "Not renaming $original_name"
                            continue
                        fi
                    
                        mv "natives/${original_name}" "natives/${native_folder_name}"
                    done
                    
                    echo "Flooding resources with default CPU natives"
                    for resource_folder in src/main/resources/*/; do
                        # Get the base folder name (instead of full path)
                        folder_name=$(basename "$resource_folder")
                    
                        # Find the matching built natives folder
                        for native_folder in natives/*/; do
                            # Only copy if this native folder EXACTLY matches (excluding -natives)
                            # This is to make sure only the CPU natives are bundled, not Vulkan
                            native_basename=$(basename "$native_folder")
                            if [[ "${native_basename%-natives}" == "$folder_name" ]]; then
                                echo "Copying from $native_folder to $resource_folder"
                                # Copy contents from the renamed native folder into the resource folder
                                cp -r "$native_folder"/* "$resource_folder"/
                                # Remove .gitkeep (and other hidden files but there shouldn't be anymore)
                                find "$resource_folder" -type f -name '.*' -exec rm -f {} \;
                            fi
                        done
                    done
            -   name: Build
                # We already built and tested for this platform so we're not testing again
                run: ./gradlew clean build publish --stacktrace -x test
            -   name: Upload JAR as artifact
                uses: actions/upload-artifact@v4
                with:
                    name: whisperjni-jar
                    path: build/libs/*.jar
            -   name: Publish to maven central
                if: github.repository == 'Jaffe2718/whisper-jni'
                run: ./gradlew jreleaserDeploy --stacktrace
                env:
                    JRELEASER_MAVENCENTRAL_STAGE: UPLOAD # why the hell doesn't publish work
                    JRELEASER_MAVENCENTRAL_USERNAME: ${{ secrets.JRELEASER_MAVENCENTRAL_USERNAME }}
                    JRELEASER_MAVENCENTRAL_PASSWORD: ${{ secrets.JRELEASER_MAVENCENTRAL_PASSWORD }}
                    JRELEASER_GPG_PASSPHRASE: ${{ secrets.JRELEASER_GPG_PASSPHRASE }}
                    JRELEASER_GPG_PUBLIC_KEY: ${{ secrets.JRELEASER_GPG_PUBLIC_KEY }}
                    JRELEASER_GPG_SECRET_KEY: ${{ secrets.JRELEASER_GPG_SECRET_KEY }}
            -   name: Create release
                if: github.repository == 'Jaffe2718/whisper-jni'
                run: |
                    cd natives
                    # Zip everything in the same dir
                    for dir in */; do
                      echo "Zipping $dir"
                      zip -rj "${dir%/}.zip" "$dir"
                    done
            -   id: set_tag_name
                run: |
                    echo "tag_name=v$(sed -n 's/^[[:space:]]*version[[:space:]]*=[[:space:]]*\(.*\)[[:space:]]*$/\1/p' gradle.properties)" >> $GITHUB_OUTPUT
            -   uses: softprops/action-gh-release@v2
                if: github.repository == 'Jaffe2718/whisper-jni'
                with:
                    draft: true
                    tag_name: ${{ steps.set_tag_name.outputs.tag_name }}
                    generate_release_notes: true
                    files: |
                        natives/*.zip
                        build/libs/*.jar
                env:
                    GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
